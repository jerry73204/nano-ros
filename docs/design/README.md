# nano-ros design RFCs

This directory is the **design source of truth**. Each file is a numbered, living RFC:
a design decision record that is edited in place as the shape settles. Phase docs in
`docs/roadmap/` are *work breakdowns* — they implement RFCs, they do not own design
rationale. **New design rationale goes in an RFC, never only in a phase doc.**

- **Finalized whole-system view:** [ARCHITECTURE.md](ARCHITECTURE.md) — narrates the system
  end-to-end and links into the RFCs below.
- **New RFC:** copy [0000-template.md](0000-template.md) to `NNNN-slug.md`, next free number.
- **Status field** tells you stable vs evolving: `Draft` (moving) · `Stable` (settled) ·
  `Superseded` (retired by a hard reversal; see `archived/`).
- **Drift rule:** flipping an RFC to `Stable` requires updating the matching
  [ARCHITECTURE.md](ARCHITECTURE.md) section in the same commit.

Each RFC carries frontmatter: `rfc`, `title`, `status`, `since`, `last-reviewed`,
`implements-tracked-by` (phase slugs), `supersedes`, `superseded-by`.

## Index

### Foundations & architecture

| RFC | Doc | Status | One-liner |
| --- | --- | --- | --- |
| 0001 | [architecture-overview](0001-architecture-overview.md) | Stable | layered crate stack + RMW + executor + board composition |
| 0002 | [rt-execution-model](0002-rt-execution-model.md) | Draft | real-time executor model (RTOS subsections pending) |
| 0003 | [rtos-integration-pattern](0003-rtos-integration-pattern.md) | Draft | universal embedded RTOS adapter pattern |
| 0004 | [configuration-and-transports](0004-configuration-and-transports.md) | Stable | unified config across single-node/workspace; `system.toml` + metadata; transports |

### RMW & data plane

| RFC | Doc | Status | One-liner |
| --- | --- | --- | --- |
| 0005 | [rmw-layer-design](0005-rmw-layer-design.md) | Stable | middleware abstraction (delink zenoh-only) |
| 0006 | [portable-rmw-platform-interface](0006-portable-rmw-platform-interface.md) | Stable | Rust trait + C vtable dual API |
| 0007 | [service-qos](0007-service-qos.md) | Stable | service/action QoS design |
| 0008 | [service-qos-gap](0008-service-qos-gap.md) | Superseded | gap analysis → resolved by RFC-0007 (Phase 189.M3.3) |
| 0009 | [bridge-topic-forwarding](0009-bridge-topic-forwarding.md) | Stable | in-binary `[[bridge]]` raw-CDR topic relay |
| 0010 | [zero-copy-raw-api](0010-zero-copy-raw-api.md) | Stable | zero-copy raw publish/subscribe API |
| 0011 | [px4-rmw-uorb](0011-px4-rmw-uorb.md) | Stable | PX4 uORB RMW backend |
| 0031 | [rmw-selection-and-lowering](0031-rmw-selection-and-lowering.md) | Stable | RMW declared (system.toml/flag) + lowered per language; per-deploy |
| 0035 | [rmw-vtable-abi](0035-rmw-vtable-abi.md) | Draft | frozen 34-slot RMW vtable ABI + append-only rule + NULL contract + `abi_version` |
| 0058 | [multi-edition-ros-test-harness](0058-multi-edition-ros-test-harness.md) | Draft | **test-only** `RosEnv` provider so codegen + interop run against MULTIPLE ROS editions (a host holds only one). Two backends behind one trait: `HostRosEnv` (default edition, host `/opt/ros/<d>`, today's `ros2.rs`) + `DockerRosEnv` (extra editions, `nano-ros-ros:<distro>` image baking distro + pinned zenoh 1.7.2 overlay + `domain_bridge`, built locally on demand). Extra editions run in **opt-in** lanes covering two axes: **codegen** (`generate` in-container → `generated-editions/<E>/`, diff vs golden) + **testing** (host builds fixture against those defs → live interop vs the edition peer + `domain_bridge`). Default loop + user build untouched; motivated by issue 0267's manual jazzy verification. Work: phase-309 |
| 0062 | [unified-dependency-ssot](0062-unified-dependency-ssot.md) | Stable | `nros-sdk-index.toml` is the SSoT for **every** dependency class. `[prereq.<key>]` is the ONE declaration namespace over four providers (system package / prebuilt dist / source / python), each with a `check` probe; unknown keys are an error. Setup AND doctor both derive from it, so remedies are computed, never hand-written — the drift class where three remedies pointed at apt while an index prebuilt existed. Sudo boundary: unprivileged classes install first, the privileged closure is composed into ONE native command and PRINTED (`--sudo` to run), so a missing OS package degrades rather than aborting the remainder (0368-F1). A `[tool.*]` declares its dist's runtime libraries with `system = [..]`, MEASURED from the dist and gated by `check-dist-runtime-deps` rather than hand-listed (0926). **The 2026-08-29 amendment REVERSED two things this row used to claim:** rosdep is not consulted at all (the fallback is deleted, not an optional backend), and `[system.*]` merged into `[prereq.*]` and retired. **Amendment 2 (2026-08-30)** decides WHICH provider wins: a tool whose output enters a build artifact is PINNED whatever the host has, a tool that must match something the host already runs comes from the SYSTEM and shipping our own is a defect (generalising RFC-0075's zenohd), and everything else prefers the system when a version constraint is satisfied — which `check` cannot express yet, so that is the gate (phase-404). Motivated by issue 0368's clean-host walk |
| 0063 | [system-model-is-a-build-artifact](0063-system-model-is-a-build-artifact.md) | Draft | The SystemModel stops being a committed source and becomes a DERIVED BUILD ARTIFACT, generated per build into `build/` (colcon-style) and exposed for inspection. Resolves the contradiction behind issue 0380: the committed model held both `resolve(inputs)` output AND hand-authored `execution.tiers` dims, so issue 0320's "delete and re-resolve" remedy destroyed data phase-296 W5 declared authoritative. Makes `resolve(inputs)` total — the dims must become expressible in user-maintained config (launch + project config + system config), which is 0380's direction A promoted from option to requirement |
| 0064 | [board-support-organization](0064-board-support-organization.md) | Draft | Board support organization + tiers; `[board.*]` in the SDK index is a package-set alias, not board support. **R3 (2026-08-04) adds the SUPPORTED MATRIX**: witnesses chosen by (integration shape × arch), keyed on the descriptor's already-declared-but-unconsumed `net_stack` axis — **rtos-owned** boards (Zephyr/NuttX/ESP-IDF/vendor SDK) cost a 0–160-line config bundle, **nanoros-owned** ones cost ~60–80 lines *plus a MAC driver*, so breadth is bought only in the first class. Tier 1 = linux, Zephyr native_sim, NuttX qemu-arm, FreeRTOS mps2-an385, ThreadX-linux; tier 2 adds a NEW Zephyr QEMU Cortex-M witness (all 28 Zephyr configs are `native_sim/native/64` with `NET_SOCKETS_OFFLOAD=y`, so Zephyr's own IP stack, a real driver and 32-bit have NEVER been exercised), NuttX riscv, ThreadX riscv64, esp32-qemu, bare-metal mps2. `stm32f4` leaves the matrix (0 Runtime cells) for the book's customization example; the 4 scaffolds contribute zero cells, so removal is free — except `orin-spe`, load-bearing as a pseudo-platform. Naming: platform stays `posix` (the seam), board becomes `linux` (the promise — all 19 CI jobs are ubuntu, and `timer_create` has no macOS fallback), `native` retired. Portability is NOT the blocker: the net ABI is per-platform, and ThreadX already runs ONE `net.c` over two backings (host-socket shim vs real NetX Duo) with no `#ifdef` |
| 0068 | [language-neutral-codegen-ir](0068-language-neutral-codegen-ir.md) | Draft | Codegen has no language-neutral IR: the derived facts a generator needs (RIHS hash, resolved refs, fixed-capacity storage, `repr(C)` layout, plainness, serialized-size) are computed inside `rosidl-codegen` and reached only by calling Rust, so adding a target language is a Rust change; and the one serializable IR (`rosidl_parser::Ast`) is captured at the PARSE stage, before any of those facts exist. Restructure into four stages — **parse → resolve → lower → render** — where Resolve adds the hash + refs (target-neutral), Lower bakes the target-concrete embedded facts (`ResolvedType ⊗ CodegenConfig ⊗ TargetProfile`, so hash-once/lower-per-target — the arm-short-enums layout is a Lower fact, never in the hash), and Render is a RUNTIME-templated (`minijinja`) consumer: a language = a data pack (`spelling.toml` + `*.jinja` + `pack.toml`), zero Rust. The correction over a hosted-stack JSON manifest is that the IR must carry storage/plainness/layout or it is useless to a `no_std`/32-bit/short-enum C/C++ emitter — content, not format, is what makes the data boundary safe. Refines RFC-0023; carries RFC-0067 identity unchanged. Work: phase-335. Issue #402 |
| 0079 | [priority-is-allocated-not-authored](0079-priority-is-allocated-not-authored.md) | Draft | **Scheduling priority is a DHCP/static-IP collision: one address space, hand-authored static assignments, addresses already held by infrastructure, and no reservation protocol.** Counted across the 5 bringups that declare tiers: 11 tiers cost **38 raw platform priority pins** (3.5 per tier, each needing the kernel's range AND direction AND where every system task already sits — the last written down nowhere, and measured only while fixing 0736). Worse, the field authored on **11 of 11 tiers is `spin_period_us`** — an executor implementation detail — while the actual contract (`period` 2, `deadline` 4) is mostly absent: the user writes the mechanism and omits the intent. Fix: the user declares a TIMING CONTRACT (`profile`, `period`, `deadline`, `on_miss`) and never a kernel number; ORDER derives deadline-monotonic with declaration-order ties (nondeterminism here is a fixture-flake generator); the PORT declares an address plan (`pool.app` + `reserved.{transport,driver,foreign}`) and a realizer ALLOCATES into it. Profiles are named for scheduling SHAPE (`realtime_periodic`/`responsive`/`throughput`/`background`), never application role — a preset called `control` bakes in a scenario, and the ROS 2 QoS profiles (already mirrored here by `QoSProfile::services_default()`) are the precedent for presets-over-expressive-core taught profiles-first. Crossing into a reserved band requires NAMING it (`above = "transport"`), which makes 0623 structural: both orderings stay legitimate, neither can be chosen by accident. **Primitive reduction to the canonical (C,T,D)**: `budget_us` is deleted as a SECOND declaration of RFC-0078's WCET — issue 0736 IS that divergence (`budget_us = 5000` vs callbacks measuring 2000-4000, tier dispatched 3 of 1250 spins), and absent must mean fixed-priority, not a guessed allowance. Also retires the 0-31 normalized scale that WAS 0623's defect, and notes `class` means three different things in one file format (2 tier vs 75 component, plus `sched_class`). Amends 0016 (priority model only) + 0052 (names the realizer its own agnostic-core section already calls for). Migration keeps all 38 pins as CHECKED static leases — a pin in a reserved band becomes a build error instead of a 96% dispatch loss found three sessions later. |
| 0078 | [wcet-is-declared-per-profile](0078-wcet-is-declared-per-profile.md) | Draft | **A measured maximum is EVIDENCE, not a bound** — the longest time observed over N runs is a high-water mark, and converting it to `exec_ms` would be issue 0259's failure in better clothes (0259 counts an ABSENT number as zero; this would count an UNDER-estimate as measured). So observation and bound are separate fields and observation ALONE converts to nothing: an explicit `bound_cycles` (static analysis) wins, else a declared `margin_percent` (~20% industrially, with "very little justification save historical confidence"), else `None`. Keyed on a named measurement PROFILE — board id and platform family both collapse into one. Declares CYCLES + `clock_hz` and converts here, the shape AMALTHEA/APP4MC reached independently (ticks ÷ frequency). Per BOUNDARY at rlm's `node/path` identity so declarations and `ChainFeasibleWithoutWcet` join by set difference. Absent stays the DEFAULT (issues 0404, 0259, 0403). |
| 0077 | [image-runtime-is-the-images-choice](0077-image-runtime-is-the-images-choice.md) | Draft | **The tree already decided `#[global_allocator]` is a per-IMAGE fact (issue 0616: "a staticlib is not a graph; it is a sealed copy of one"), enforced by `check-archive-lang-items`. This RFC finishes the job for `#[panic_handler]` — and finds the ABI gap underneath it: `<nros/platform.h>` has ALLOCATION and NO FATAL PATH, so C and Rust share one arena through `nros_platform_alloc` while each language invented its own ending.** `ARCHITECTURE.md` §2 (which predates 0616) collapses IMPLEMENTATION (the platform's — §2 right), INSTALLATION (the link root's — 0616 settled), and POLICY (the image's — owned by nobody). **The two halves need OPPOSITE treatment**: panic is a FREE CHOICE (spin / halt / print-and-exit / log-then-reboot are all implementable everywhere; which is right depends what the image IS), while the allocator is NOT a choice but a PLACEMENT — the image picks where the static lives, never what backs it, because zenoh-pico's `z_malloc`, CycloneDDS's `ddsrt_malloc` and the RTOS draw from the same heap and a second Rust arena would fragment memory nothing can measure while falsifying RFC-0034 D7's `heap_used_bytes` ("one API, one arena" is already the adapter's comment). Proposes `_Noreturn nros_platform_panic(msg, len)` in the ABI, each port mapping it to the fatal path it already has (`k_panic`, `PANIC()`, `esp_system_abort`, the FreeRTOS hook, `abort()`), so a Rust panic, a C precondition failure and a C++ terminate converge on ONE symbol the image controls — the same weak-default-the-application-overrides shape Zephyr (`k_sys_fatal_error_handler`), FreeRTOS, NuttX (`board_crashdump`) and ESP-IDF all settled on independently. **One rule, three shapes: the package that DECLARES THE ENTRY owns the image runtime** (`*_entry` in a workspace, the example package standalone, `nano_ros_entry()`'s TU for C/C++), configured natively per language — `use panic_halt as _;` in Rust, a strong definition over the port's weak one in C, or `nano_ros_entry(... PANIC halt)` for stock behaviour. Evidence that policy cannot live in a library: FOUR hardcoded behaviours already exist (`nros-c` spins SILENTLY, nuttx prints+exit(1), threadx-rv64 UART+exit, freertos semihosting+`bkpt`+spin) — four reasonable answers to "is a human watching? is a debugger attached? is a harness grepping stdout?". Corrects earlier drafts of itself twice: it framed this as opposing §2 when the tree had already moved, and it rejected splitting panic from allocator using an INSTALLATION argument against a POLICY question. Gate: extend `check-archive-lang-items` to the panic lang item and count per image COORDINATE — per link line catches duplicates but cannot catch ABSENCE, which is what #0617's `#[panic_handler] function required` was |
| 0076 | [platform-abi-ask-do-not-assume](0076-platform-abi-ask-do-not-assume.md) | Draft | **The platform ABI cannot say how big its own objects are, what a task should look like, or that a platform simply does not do something — so callers guess, and the guesses are checked by nobody.** Two phase-359 items hit this in one week and both worked AROUND the ABI: W7 wrote a bespoke C shim (`nros_nuttx_spawn_tier`) because there is no portable way to ask for a stack size, and W10 had to add `task_storage_{size,align}` before it could allocate task storage at all. Five defects, each with its receipt: (D1) only `wake` and `task` can be sized, so `zpico-sys`'s generic adapter carries a HAND-COMPUTED table of other platforms' struct sizes with `≈` and a "2× safety margin" — issue 0570's exact shape; (D2) `attr` is undefined, IGNORED by posix/zephyr, a private struct on freertos/esp-idf, and MANDATORY on threadx, so W10's `PlatformTask` passing `NULL` is a guaranteed `-1` there, and `stack_depth` means words on FreeRTOS and bytes elsewhere; (D3) the declared `NROS_PLATFORM_RET_*` vocabulary is used by NO port, so "never supported here" and "failed just now" are the same `-1` — which is why W10's pool permanently disables a priority level on what issue 0246 says is a TRANSIENT failure — and below that, capability is negotiated by LINK ERROR; (D4) two provider kinds (hand-written `platform.c` vs the `nros_platform_export!` macro over trait impls) and nothing keeps them in step — W10 added the task probes to the header and all five C ports but not the macro, so the three Rust ports lack them on `main` today; (D5) `os_pri` is "platform-native" and the natives disagree — 0 is HIGHEST on ThreadX and LOWEST on FreeRTOS, for a number authored once in `system.toml`. Proposes: storage sizing in BOTH forms callers need (runtime probe AND compile-time macro, tied by a `_Static_assert`, because zenoh-pico embeds these types by value and a function call cannot size an array); a public `nros_platform_task_attr_t` where `NULL` means defaults on EVERY port and `stack_bytes` is always bytes; the return vocabulary actually used, narrowed to the `int8_t` the functions already return, with `UNSUPPORTED` vs `NOMEM` distinguished because one is cacheable forever and the other must be retried; the export macro's symbol list GENERATED from the header so D4 becomes unrepresentable; and a normalised priority band with a raw escape hatch |
| 0075 | [zenoh-router-provenance-and-the-unstable-seam](0075-zenoh-router-provenance-and-the-unstable-seam.md) | Draft | **We pin against the layer that promises stability and test against the layer that does not — today it is the other way round.** nano-ros builds its own `zenohd` pinned to zenoh 1.7.2 "for rmw_zenoh_cpp compat", but the zenoh inside `ros-humble-rmw-zenoh-cpp` moves WITHIN one ROS distro: 0.1.1 → 0.1.9 took it 1.2.0 → 1.8.0, and interop went from zero samples to 10/10 without our pin participating (issue 0609). It could not have: zenoh's WIRE is guaranteed backward-compatible across 1.x (`Z_PROTO_VERSION 0x09` is what that covers), while `rmw_zenoh`'s keyexpr / liveliness-token / attachment / QoS-packing conventions — the layer we actually bind to — are specified in its `design.md` with **no stability statement, no version field, and no subject-to-change note**, shipped as pre-1.0 `0.1.x`. So no pin can defend that seam. Proposes: take the router from ROS (`rmw_zenohd`, which links the SAME `libzenohc.so` the RMW does — a lockstep, not a pin), keep vendoring `zenoh-pico` because it compiles into firmware and the 1.x wire guarantee makes a fixed pin safe there, retire the vendored `zenohd`, and replace pinning with a CONFORMANCE gate that reports both versions on failure — read from `zenoh_configure.h`, never `dpkg -l`, whose `0.1.9` is a ROS wrapper version saying nothing about the 1.8.0 inside. The argument that needs no version number: **62 test files spawn our router and 2 spawn the one users actually deploy** |
| 0074 | [ingress-budget-rate-and-burst](0074-ingress-budget-rate-and-burst.md) | Draft | **The contract bounds what a device's own tasks may do and says nothing about what a remote publisher may send it** — and on a device whose transport band sits above its tiers (the correct default; the inverse starves the RX drain into multi-second RTO freezes, d708d8c5b) that hole lets a peer preempt every real-time tier, with no local scheduling fix. Measured on the FreeRTOS mps2 lane: a ~2 kHz flood stalls the island 9-11 times per 30 s, worst gap 335-511 ms against 10-100 ms declared periods, while a Tonbandgeraet trace attributes 72-81% of CPU to `zpico_read` and 2.3-3.0% to all three tiers combined (lwIP's `tcpip_thread` under 2% — so it is the rmw drain, not the IP stack). **The cause is BURSTINESS, not average rate**: a router cap set AT the offered rate (2000 Hz vs ~2 kHz) still removes every stall and then carries 693-752 msg/s against 157-265 uncapped — the island processes MORE when paced than when flooded, i.e. congestion collapse rather than saturation — so the declaration is a **token bucket (rate + burst)** with the burst term load-bearing, and the earlier '~750 msg/s drain envelope' is demoted to a description of bursty traffic. Two enforcement points: a **router-side pacing rule**, the only mechanism measured to fix BOTH the cadence and the chain-delivery harm (receiver-side dropping cannot — the ring-depth probe left stalls intact because a ring drop happens after the transport decoded the message), and a **device-side drain budget**, specified but BLOCKED on #0567 (zenoh-pico resets its rx buffer every call, so an early return discards frames — measured, a 4/16-frame cap improves cadence while collapsing delivery 282 -> 10 msg/s, which is a drop policy wearing a budget's clothes). Rejects ring depth, TCP_WND shaping (fixes cadence, drives chain p50 to 446 ms), and a burst-less rate cap. Message priority is complementary, not an alternative: it reorders the link, it cannot reduce a per-message decode cost |
| 0073 | [platform-clock-nanoseconds-and-resolution](0073-platform-clock-nanoseconds-and-resolution.md) | Draft | **The platform clock ABI fixes a UNIT but cannot express RESOLUTION**, so every port either lies or truncates: microseconds are a lie on ThreadX (100 Hz tick = 10 ms steps under a us signature) and the mps2 port (`clock_ms() * 1000`), while nanoseconds are discarded on POSIX and on hardware resolving 40 ns (mps2 SysTick, measured) or 6 ns (STM32F4 DWT) — and the same header already advertises ns for the WALL clock. Issues 0502, 0515 and 0531 are three symptoms of the one missing fact; 0531 in particular is a clock that returned 0 forever on Cortex-M Zephyr under 60 MHz, undetected because **nothing in the tree asserts that a clock advances**. Replaces `clock_ms` + `clock_us` with `clock_ns` + `clock_resolution_ns`, unit conversions demoted to `static inline` header wrappers (the ABI mirror gate already skips those by design). **The cost objection is measured and refuted**: on mps2 under `-icount shift=0`, per read us=91, ns naive=94, ns with an exact per-cycle multiplier=37, raw-counter+frequency=34 instructions — the unit is free, the runtime DIVISION is the cost, and ns-per-cycle is an integer at 25/50/100/125/200/250 MHz while us-per-cycle never is above 1 MHz, so a microsecond ABI is forced into a divide exactly where a nanosecond one gets a multiply. Today's us path is thus the most expensive option measured, and the executor reads it every spin. Rejects **Linux-style clock ids** (un-implementable with fidelity on 4 of 6 backends — no ids at all on FreeRTOS/ThreadX/ESP-IDF, and NuttX's `clock_getres` returns one tick-derived number for every id) and **raw counter + frequency** (3 instructions cheaper, pushes wrap handling and conversion onto every caller). Defers a coarse variant until a caller proves it needs one. Adds three conformance cases, one of which — *the clock advances* — would have caught 0531 |
| 0072 | [rtos-integration-nano-ros-is-a-guest](0072-rtos-integration-nano-ros-is-a-guest.md) | Draft | **nano-ros is a library the user's project imports; the RTOS owns its own build.** Already true for 3 of 5 platforms, in ONE shape across three languages — build glue + Kconfig + the host's package manifest (`zephyr/module.yml`, `integrations/nuttx/Make.defs` with the staticlib on `EXTRA_LIBS`, `integrations/nano-ros/idf_component.yml`). **FreeRTOS is the anomaly**: no shell, and `nros_freertos_build_kernel()` compiles the kernel from `FREERTOS_DIR` + `FREERTOS_PORT` — a choice that dates from when upstream shipped no build system, which it now does. Grounded in a survey of SIX real distributions: **six conventions for where `FreeRTOSConfig.h` comes from, five for port selection**, and only 3 of 6 expose linkable targets (Pico renames all of them and ignores `freertos_config`; **NXP inverts control** — the app `include()`s the SDK root and everything is `mcux_add_source()` onto one target, so there is no library to link). ST ships FreeRTOS and lwIP as submodules of separate repos, its lwIP port talks to the CMSIS-RTOS shim rather than FreeRTOS, and `ethernetif.c` is per-example app source welded to the HAL, a BSP PHY and linker-script sections. **`freertos-lwip` dies**: it collapses two orthogonal facts, and our own dependency models them better — zenoh-pico ships `system/freertos/{system.c,lwip/network.c,freertos_plus_tcp/network.c}`, so **FreeRTOS+TCP is vendored and unreachable**, its only mention under `config/` a comment documenting a hand-tuned cross-pairing. The stack becomes a FACT the user states (`netstack = "lwip"`), capabilities are the contract (ESP-IDF's `ESP_NETIF_USES_TCPIP_WITH_BSD_API` pattern — consumers test a capability, the choice names the impl), and `[build.zenoh]` leaves every platform file for the backend descriptor keyed by `(rtos, netstack)`, extending phase-347's *core names no backends* to **platforms name no backends**. Deliberately NOT proposed: a `netstack` provider KIND with selection — every vendor has welded its choice, so there is nothing to select; capabilities give the decoupling and selection waits for a real second stack on one RTOS. Vendor knowledge lives in the USER's workspace (phase-348 discovery finds it), so we ship descriptors only for what we test. Retires `nros_freertos_build_kernel()` in favour of upstream's own `CMakeLists.txt`, which also kills a live collision: `FREERTOS_PORT` is upstream's name taking an ENUM (`GCC_ARM_CM3`) while ours takes a PATH fragment (`GCC/ARM_CM3`). Work: **phase-349** |
| 0071 | [rmw-backend-descriptor](0071-rmw-backend-descriptor.md) | Draft | A backend ships an `nros-rmw.toml` declaring its build facts and capabilities; the toolchain lowers it the way `nros-board.toml` is already lowered (RFC-0042 D2, `NanoRosCapabilities.cmake`) — so **core names no backend**. "CycloneDDS is the only exception" is wrong twice: RFC-0031 says it is NOT pure-cargo linkable (the opposite of the usual gloss), and 3 of 4 backends are already non-cargo — `nros-rmw-xrce` is **C**, `nros-rmw-uorb` is **C++**, cyclone is mixed. What makes cyclone feel singular is that it alone demands **per-message-type work**, and that demand leaked into core. Key finding: **the capability is already generic, only the trigger is backend-named** — `nros-node/build.rs` flips `cfg(rmw_needs_type_descriptors)` from `CARGO_FEATURE___CYCLONEDDS_LINK` while the seam it feeds (`MessageForRmw`, `register_type_descriptor`) is capability-shaped; phase-248 C2 got the seam right and left the trigger. So: capability features (`rmw-present`, `needs-type-descriptors`) replace backend detection, the generated selection facade carries the lowering (no user manifest changes), `resolve_rmw`'s closed `KNOWN_RMW` match and its generated `if/elseif` CMake dispatch become a descriptor read, and `[rmw.codegen].per_message` names the per-message hook that `NanoRosGenerateInterfaces.cmake` hardcodes for cyclone in 27 places. Evidence the closed list has already failed: `nros_rmw_dispatch` reports "known: zenoh xrce cyclonedds" — **`uorb` is missing**. Rejects requiring a Cargo manifest per backend (xrce/uorb are C/C++ and must stay so). Acceptance is a fifth backend added out-of-tree with zero core edits. Work: **phase-347** (W1 subtractions, W2 descriptor + agreement gate, W3 retire all three lists, W4 capabilities both lowerings, W5 the codegen hook, W6 the platform leak; the colcon convention is **phase-348**, blocked on 347 W2). **Open questions answered 2026-08-10**: **discovery is BY NAME over a SEARCH PATH OF WORKSPACES, scanned at SOURCE TIME** — `RMW_IMPLEMENTATION` moved to build time, so `KNOWN_RMW`/`canonical_rmw`/the generated `if/elseif` DISAPPEAR rather than move. **Why not colcon:** its index is reached by sourcing `setup.sh` and so exists only after an INSTALL step, and per-target static RTOS artifacts have no such stage — source-time scanning is forced by the target, not chosen. The path is an ordered list of workspace roots defaulting to `[<nano-ros root>, <user workspace>]`, with the nano-ros tree as the FIRST ENTRY rather than a special case; only those two sources are accepted (no `~/.nros` index, no env var) so a build is reproducible from the checkout alone — which costs nothing today, since nros-cpp has no `install()` and the `NanoRosCppTargets.cmake` its comments cite is not in the tree. Deliberately unfinished: nano-ros providers must gain `package.xml` (8 rmw families, 17 board dirs, 14 platform dirs; only 8 boards carry a descriptor today), so a provider without one is simply not discoverable and existing paths keep working; `has_rmw`'s four-way test is **3/4 DEAD** — `nros-node` declares no `rmw-zenoh`/`rmw-xrce`/`rmw-uorb` feature, so it is already `rmw-cffi` alone (and its "also true under cfg(test)" comment describes a branch that does not exist); uorb's absence is a real gap and the tree has **THREE disagreeing lists** — `NanoRosFeatureSet` accepts uorb, `nros-cpp/CMakeLists.txt` handles it, `nros_rmw_dispatch` fatals on it, and it survives only because the PX4 shell links the target directly and never takes that path; `unstable-zenoh-api` on core `nros-node` is DEAD (no manifest forwards it, no `cfg` reads it — the live chain is entirely in the backend), so it is a deletion, and **D6** answers the general form: a user selects a CAPABILITY (`zero-copy-receive`) which the backend descriptor maps to its own feature, mirroring boards' `capability_features`; uorb is **not** special — it behaves like every other backend and only its PX4 consumer is unusual; and 0493 goes FIRST (both rewrite the nros-c/nros-cpp feature tables, and 0493's linking model is still unexplained) |
| 0070 | [build-cache-layout](0070-build-cache-layout.md) | Draft | Build caches grew as suffix-named siblings of their sources: **117 `target*` + 249 `build*` dirs inside source trees, and 236 hardcoded cache-path literals across 17 files** (justfile, five `just/*.just`, six `scripts/build/*`, `fixtures.toml`, the staleness gate, three Rust fixture resolvers). Three spellings encode "which RMW" alone. Four rules: **R1** one root — everything under `$NROS_BUILD_ROOT`, nothing inside a source dir; **R2** one vocabulary — `<kind>/<coordinate>` from the fixture-manifest coordinate (platform, lang, rmw, feature-sig), a new ad-hoc suffix is a bug; **R3** one derivation — build, staleness gate and test resolver call ONE function and never spell a literal (the #393 rule applied to paths; `fixtures-target-dir.sh` is the working precedent, generalise it rather than adding a second); **R4** sharing comes from one invocation over many packages OR from N invocations against one dir, and **which is right is a measurement, not a principle** (amended 2026-08-08 — the original "N concurrent invocations are a net loss" cited an A/B that varied `incremental`, not sharing; measured over 37 leaves in one group, N invocations against one dir are 1.9× faster than N dirs and remove 21.8:1 of bytes). Prefer the shape needing no generated state unless wall clock binds: both dedupe identically, and bytes are what R1 is for. Unblocks corrosion's 9:1 duplication (32.6 GiB / 21 dirs; ~25 % disk saving) by giving those trees an addressable root. Migration is ordered derivation → callers → paths → gate, each family's build+probe+resolver in ONE commit. Work: phase-334 W2 |
| 0069 | [action-payload-envelope](0069-action-payload-envelope.md) | Draft | The action feedback/result payload carries ONE CDR encapsulation header — ROS 2's — not the two nano-ros's raw path emits today. Filed from issue 0418: `[outer][goal_id][INNER][body]` is self-consistent raw↔raw (every action Runtime cell is raw↔raw, so it cancels) and incompatible with `rcl_action` peers AND with nano-ros's own typed path. The inner header is deliberate — the raw consumer reads the body with `new_with_header` — so producer and consumer must change in one commit or the issue-#35 corruption returns. Options weighed: adopt ROS 2's envelope (recommended, breaks nano-ros↔nano-ros across the version boundary), keep it and document actions as non-interoperable, or translate at the boundary (rejected: needs a peer discriminator the wire does not provide). Gate is the embedded lanes, which are the raw↔raw pairs the change breaks and must re-prove |
| 0067 | [env-invariant-msg-dep-identity](0067-env-invariant-msg-dep-identity.md) | Draft | A committed generated-message-crate reference has two axes that vary with the host ROS distro, and both break something: the **ament version** (`std_msgs` = 4.9.1 jazzy / 5.3.6 rolling) makes a committed `Cargo.lock` drift → `--locked` fails on every other distro; the **crates.io registry source** (`std_msgs = { version = "*" }` + `[patch.crates-io]`) resolves against PUBLIC crates.io when the patch isn't in the loaded config chain, where third parties own those names (issue 0378) — and `0.0.0`-constant alone makes it WORSE (`std_msgs = "0.0.0"` is squatted). Decision: make both axes env-invariant from `package.xml` — message deps become `path` deps (no crates.io name anywhere → fails closed, never the registry) + generated crate stays `version = "0.0.0"` (ament → metadata) → committed lock byte-identical across distros. Prototype-validated on `int32-sink`: builds, unifies to one copy, lock pins `0.0.0` no registry source, from-root resolves `path+file://` not crates.io — refutes issue 0378's "no repo-side config can fix it". Work: phase-333 |
| 0066 | [example-and-fixture-consolidation](0066-example-and-fixture-consolidation.md) | Draft | The example tree's two populations swapped jobs: feature coverage is expressed as DIRECTORIES (28 themed micro-workspaces, one per feature × language) while build configuration is barely expressed at all (84 of 86 workspace fixture rows are zenoh). Measured 2026-08-02: **34 of 42 themed workspace rows declare no `features`, no `cmake_defs`, no `env`** — a themed workspace is not a config variant, it is a directory holding one different node package. Invert both: a feature (QoS/params/lifecycle/custom-msg/remap) becomes a node package inside the four large workspaces, which already carry pubsub+service+action across six platforms; a configuration (rmw, feature set) becomes a fixture AXIS over them. 32 workspace dirs → 11, and the RMW axis reaches workspaces for the first time. Behavioural outliers stay separate (`safety` = build feature changing probed ABI sizes; `realtime` = own toolchain pin, only multi-platform theme; `sizing` = its value IS the zero-callback-entity launch of issue 0257; `bridge` = only non-zenoh rows). **uORB is NOT an axis value** — no CDR, and it models neither services nor actions (RFC-0011) while the large workspaces contain both, so the cell is unbuildable; PX4 is a CarveOut with zero fixture rows and stays with phase-325. Board-tier extraction deliberately excluded: tier 3 is 2 % of fixture rows, so it is a maintenance-surface win (RFC-0064), not a build-time one |
| 0065 | [colcon-like-workspace-builder](0065-colcon-like-workspace-builder.md) | Draft | `nros build` becomes the workspace entry point: discover → resolve → preflight → generate → **exec**, writing the root build file and handing off to cargo/cmake/west with stderr untouched. Amends RFC-0024 §2.4 in SCOPE — the rejection was of WRAPPING the compiler, and an `exec` does not wrap. Reusing colcon is rejected on RFC-0070's measurement: its per-package configure fragments the corrosion cargo tree. Entry × board → image, so the ENTRY is generated (escapes are declarative `panic`/`profile` keys; `nros materialize` is a marked one-way last resort, after Expo retired `eject`) and `[deploy.*]` becomes `[image.*]`. A board is a (machine, platform) PAIR — `PlatformId` already is that list, and phase-337 W2 paid five defects to learn it. Overlays reach each framework through its own external-config knob (`APPLICATION_CONFIG_DIR`, `SDKCONFIG_DEFAULTS`, `CONFIG_APPS_DIR`), never a path whitelist. Output is `dist/<image>/` — a SET plus a manifest, because Zephyr sysbuild's standard case is app + MCUboot |
| 0061 | [fixture-freshness-and-test-tiers](0061-fixture-freshness-and-test-tiers.md) | Draft | Two changes to what a test run costs. (1) The workspace-fixture signature hashes the CLI **binary** (`sha256` of `target/release/nros`, issue #182); Rust binaries are not reproducible across rebuilds, so every `just setup-cli` invalidates every workspace fixture — measured 2026-07-28: a codegen change that only ADDED a rejection path no fixture uses invalidated **40 fixtures / 35 build dirs**, a multi-hour ~100 GB rebuild whose correct answer was zero. Replace with a **toolchain fingerprint** — hash of what `nros` AND `nros-launch-resolve` EMIT over committed probe corpora, cached per binary, resolver half scoped to records with a bringup (the resolver emits SystemModels, which ARE fixture inputs; both 2026-07-28 skews — the rejected `--bringup-root` and the absolute→relative model paths of #320 — were invisible to a signature that hashes only `nros`). The corpus doubles as a seconds-cost codegen golden test (it caught a ser/deser macro swap and a trailing-newline change during 0344–0346). (2) `just ci` is `check + rust-rtos-link-check + test-all` and `test-all` gates on EVERY platform's fixtures, so a native-intent run is blocked by a stale ThreadX fixture. Tiers 0–3, each tier's gate scoped to its own fixtures; **tier 2 = pairwise set cover over the DECLARED cells** (axes aren't independent) on platform×lang×rmw×kind = **37 of 182 runtime cells (20%)**, measured — full 5-axis pairwise is 73 cells (40%) and was rejected, since no interaction defect found this session was workload-specific. Routing by two computable signals (executor storage size, toolchain fingerprint). Corollaries: per-family artifact drop (~800 GB sweep, hit 11 MB free twice), QEMU concurrency cap (287-W7), local≡remote lanes (#337: 60 red runs) |
| 0060 | [launch-toolchain-three-layers](0060-launch-toolchain-three-layers.md) | Stable | split the launch toolchain into THREE repos on the line of what a consumer can link: `ros-launch-manifest` (spec/theory/proofs/algorithms — no ROS, no Python), **`ros-launch-resolve`** (NEW — launch tree → SystemModel, + CPython, no rclrs/colcon), `play_launch` (Linux runtime + binary). Descendants pin layer 2 only, so the colcon prerequisite and the double-vendored spec both disappear rather than being feature-gated around. No cycle: `sched` takes a pre-flattened FQN-string `MapperInput`, so ROS-shaped work stays in the adapters. `check` (z3) stays in rlm — crate isolation already keeps it out of consumers' graphs, and layer 2 pays it either way |
| 0059 | [launch-toolchain-split](0059-launch-toolchain-split.md) | Superseded by RFC-0060 | Split the launch toolchain: keep only CPython-requiring stages in a **separately-built** tool (built on the user's machine against the user's Python, the way `just setup-cli` builds `nros`), and LINK the Python-free resolve pipeline into our own toolchain. The seam becomes a committable expanded-launch **IR artifact**, not a CLI verb — which is what removes the unbounded version skew of issue 0285 rather than managing it. Measured motivation: all **101** tracked `examples/**/*.launch.*` are XML, **zero** use `$(eval …)`, so the whole in-tree corpus needs no interpreter — yet is blocked today behind a tool that embeds one unconditionally. Real Python boundary is NOT "XML vs .py": `$(eval)` reaches CPython from XML and YAML equally. Open decisions: `$(eval)` policy, the fate of the duplicate in-tree `nros-launch-parser`, whether z3-bearing `check` follows |
| 0057 | [ament-parity-component-registration](0057-ament-parity-component-registration.md) | Stable | ament-parity registration surface: split `nano_ros_auto_add_library` + `nros_components_register_node(target PLUGIN … EXECUTABLE …)` (keyword parity with rclcpp_components — upstream CMakeLists ports by macro-name swap); **retire Phase 212.L.4** (class prefix == pkg) — cmake writes `pkg: ${PROJECT_NAME}` into nros-metadata.json, metadata.rs keys on it, nested upstream namespaces (`autoware::x::Class`) port verbatim (issue 0275); `nano_ros_auto_add_library` auto-wires generated interface deps, hiding the topo-last superset-archive footgun (issue 0277 UX; porting-notes 02/08/12). `SHAPE` defaults rclcpp; `HEADER` stays the one nano-specific optional keyword (static-linking price, convention-derived). Legacy macros remain as compat spellings |
| 0056 | [ros-edition-axis](0056-ros-edition-axis.md) | Draft | formalize **ROS edition** (target ROS 2 distro) as the third build axis (RMW × platform × **edition**) — a functional feature ordinary crates may branch on (unlike RMW/platform). One **per-edition interop profile** captures what varies on the wire across distros: **type hash** (Humble `TypeHashNotSupported` → Iron+ RIHS01, phase-41), wire encoding/extensibility (**REFUTED as an edition field 2026-07-26** — jazzy is FINAL/XCDR1 like humble; extensibility is per-type, RFC-0055 parked), and the **interface set** (`generated/<edition>/`). Extends the enum `humble`/`iron` → `+jazzy`/`+rolling`; selection via the `ros-<edition>` feature (as `nros-rmw-zenoh::keyexpr` already does). Turns "match the peer's distro" into one first-class selection rather than a per-call guess. Work: phase-41 (type hash) + phase-303 (encoding) |
| 0055 | [wire-encoding-xcdr2-extensibility](0055-wire-encoding-xcdr2-extensibility.md) | Draft | extend the wire encoding beyond XCDR1-only to **XCDR2 + explicit type extensibility** for modern ROS 2 (humble+) interop: `nros-serdes` gains an encoding-version-aware writer/reader that emits/parses the **DHEADER** for `@appendable` nested structs (encapsulation `0x0008/0x0009`), codegen declares extensibility explicitly (`nros-msg-to-idl` `@appendable`/`@final` → the Cyclone descriptor becomes correct for free; `rosidl-codegen` emits the matching Rust `EXTENSIBILITY` const), + `data_representation` QoS negotiation (advertise `[XCDR2, XCDR1]`, fall back for legacy peers). **PARKED (2026-07-26): premise REFUTED** — a default jazzy peer is FINAL/XCDR1 (not XCDR2/appendable), and #0267 was fixed in the Cyclone descriptor builder (`m_size` + preorder sibling-skip), NOT via a missing DHEADER. The XCDR2 machinery stays built+tested for a future PER-TYPE `@appendable` opt-in; it is not on the default interop path. Records the adjacent gaps: big-endian read, type-hash/RIHS. Non-goal: `@mutable`/optional (PL_CDR2/EMHEADER). Work: phase-303 |
| 0038 | [zero-copy-data-transport](0038-zero-copy-data-transport.md) | Stable | single-copy receive: size-class buffers + in-place dispatch; removes copy #1 + arena buffer (issue #8 / Phase 231) |
| 0039 | [px4-integration-architecture](0039-px4-integration-architecture.md) | Draft | PX4 umbrella: in-firmware uORB vs companion XRCE; 1.16 message versioning; px4-rs revision opportunities (RFC-0011 = uORB detail) |
| 0088 | [serialization-format-is-a-compile-time-provider](0088-serialization-format-is-a-compile-time-provider.md) | Draft | ROS 2's format string is a **`dlopen` key** (`rosidl_typesupport_c` loads a shared library when the identifier differs) and neither of its typesupport ABIs is stable — fastrtps passes `eprosima::fastcdr::Cdr&` (a third-party C++ type) through a `dlopen`ed vtable, introspection is clean C with no version or size field while members were appended across distros. nano-ros links one image, so **format becomes a TYPE**: a message declares one, a backend declares one, mismatch is a compile error, and the publish path gains no branch. The `u8` discriminant is IMAGE-LOCAL (we cannot allocate one to a third party) and the STRING is the cross-image identity. The bridge is the only runtime site — `open_multi` erases the type-level answer, so `PubSubBridge::new` compares one byte at construction and refuses a mismatch without a `SerializationFormatConverter`, turning nros-bridge's "both sides must use ROS-CDR" comment into a return value. Activates the vtable slot `rmw_vtable.h` already RESERVED for exactly this ("reserved so a bridge image linking two backends can [ask]"). A user's serializer is a provider package, family `serdes`, defaulting to a schema-driven strategy over the `&'static [Field]` schema codegen already emits — so a custom format costs no codegen. Keeps ROS 2's semantics (one backend, one encoding, never user-selected per topic). Work: phase-421 |
| 0090 | [codegen-version-is-the-compatibility-token](0090-codegen-version-is-the-compatibility-token.md) | Draft | Generated code and the runtime it compiles against must agree, and nothing said so: a shipped `nros` binary whose emitters had moved emitted code that COMPILED and was wrong. One authored integer — the **codegen version** — is the token. The runtime declares `[MIN, N]`, the binary declares what it emits, every generated artifact carries what it was emitted at; `E` out of range refuses at `nros build` START (before a byte is emitted) and `G` out of range refuses naming the DIRECTION, which is the difference between "regenerate silently" and "a human must move a pin". Not a hash: a hash says "different" (no direction) and moves on a doc comment (so users learn to ignore it); the hash survives only as the ratchet's evidence, never compared at build time. Asserted with the two mechanisms already load-bearing here — a `const` block per RFC-0088's `format_check` in Rust, and `variant_symbol.c`'s weak anchor symbol in C/C++, where the symbol NAME is the whole payload so no comparison logic can be wrong. Splits **freshness** (`codegen_fingerprint`, regenerate silently) from **compatibility** (the version, refuse) — conflating them is why issue 1018's refusal both over-fires on `cmd/doctor.rs` and cannot ship. Unblocks the prebuilt binary, which is issue 0171's "single highest-leverage unlock". Work: phase-429 |

### Platform, board & toolchain

| RFC | Doc | Status | One-liner |
| --- | --- | --- | --- |
| 0012 | [board-bsp-integration-architecture](0012-board-bsp-integration-architecture.md) | Stable | vendor BSP × board × SDK-variant integration shape |
| 0013 | [custom-board-provisioning](0013-custom-board-provisioning.md) | Superseded by RFC-0087 / RFC-0062 | out-of-tree boards self-describe deps to `nros setup` — the question is deleted rather than answered: under RFC-0087 a board is an ordinary `package.xml` package and there is no index lookup to extend |
| 0014 | [nros-setup-toolchain-management](0014-nros-setup-toolchain-management.md) | Stable | `nros setup` as single toolchain entrypoint |
| 0015 | [rtos-orchestration](0015-rtos-orchestration.md) | Stable | launch tree + manifest codegen across RTOSes |
| 0016 | [rtos-scheduling-features](0016-rtos-scheduling-features.md) | Stable | per-RTOS scheduling feature survey |
| 0017 | [platform-timer](0017-platform-timer.md) | Stable | `PlatformTimer` + `AtomicSporadicState` |
| 0034 | [platform-layer-split](0034-platform-layer-split.md) | Draft | enforce `nros_platform_*` as sole system boundary; alloc-first unification; scalar vs opaque-struct ABI |
| 0042 | [platform-build-determinism](0042-platform-build-determinism.md) | Draft | one canonical `<nros/platform.h>`; capability-driven config SSoT in `nros-board.toml`; generated deterministic link manifest (one register path, no `--allow-multiple-definition`/`-u`); merge-time platform×lang gate |

### Language APIs

| RFC | Doc | Status | One-liner |
| --- | --- | --- | --- |
| 0018 | [cpp-api-design](0018-cpp-api-design.md) | Stable | C++ surface mirroring rclcpp over typed FFI |
| 0019 | [nros-c-thin-wrapper-discipline](0019-nros-c-thin-wrapper-discipline.md) | Stable | nros-c delegates, never re-impls |
| 0020 | [thin-wrapper-audit](0020-thin-wrapper-audit.md) | Stable | nros-c / nros-cpp thin-wrapper compliance audit |
| 0021 | [blocking-api-rules](0021-blocking-api-rules.md) | Stable | every blocking helper takes an executor handle |
| 0022 | [entity-api-tiers](0022-entity-api-tiers.md) | Stable | convenient `fork` + customizable `clone` entity ctors |
| 0036 | [ros2-api-divergences](0036-ros2-api-divergences.md) | Draft | authoritative catalog of nano-ros vs rclrs/rclcpp/rclc divergences + rationale |
| 0037 | [rust-c-user-api-surface](0037-rust-c-user-api-surface.md) | Draft | records the Rust (`nros-node`) + C (`nros-c`) user surfaces; C++ is 0018 |
| 0089 | [ros2-api-adoption-and-the-compile-or-conform-rule](0089-ros2-api-adoption-and-the-compile-or-conform-rule.md) | Draft | when our API may take ROS 2's NAMES: adopt / adopt-bounded / refuse-loud / absent, never compile-and-differ |

### Codegen, workspace & user workflow

| RFC | Doc | Status | One-liner |
| --- | --- | --- | --- |
| 0023 | [codegen-workspace-discovery](0023-codegen-workspace-discovery.md) | Stable | unmodified ROS 2 msg pkg builds against nano-ros |
| 0024 | [multi-node-workspace-layout](0024-multi-node-workspace-layout.md) | Draft | overall multi-node workspace shape + open questions |
| 0025 | [workspace-layout-by-case](0025-workspace-layout-by-case.md) | Draft | concrete file trees per case (single/multi × rust/cpp) |
| 0026 | [example-directory-layout](0026-example-directory-layout.md) | Stable | canonical `examples/<plat>/<lang>/<example>/` shape |
| 0027 | [ros2-user-workflow](0027-ros2-user-workflow.md) | Stable | user-facing workflow + `nros new` scaffolding |
| 0030 | [sequence-of-nested](0030-sequence-of-nested.md) | Draft | nested-sequence message handling (Phase 212 spike) |
| 0032 | [entry-codegen-pipeline](0032-entry-codegen-pipeline.md) | Draft | how `main()` is emitted: proc-macro + CLI mirror, BoardEntry run/run_tiers, tier resolver |
| 0033 | [message-field-capacity-config](0033-message-field-capacity-config.md) | Stable | per-field, language-agnostic message capacity via `nros-codegen.toml`; owned/heap/borrowed modes |
| 0040 | [distribution-and-scaffolding-deps](0040-distribution-and-scaffolding-deps.md) | Draft | no-crates.io source-release model; `nros new` dep convention: in-tree + out-of-tree both use the ws-sync patch-block (git deps opt-in); CLI install line |
| 0041 | [unified-callback-receive-model](0041-unified-callback-receive-model.md) | Draft | service/action clients move from poll/`Promise`-single-buffer to callback-at-spin + QoS-depth `BufferStrategy` (triple/ring), unifying with subscriptions; dual-mode (Promise kept); ROS-aligned (KEEP_LAST 10, rclcpp callbacks); RT-safe + poll-backend (XRCE) compatible |
| 0043 | [entry-real-callback-binding](0043-entry-real-callback-binding.md) | Draft | resolves RFC-0032 §8a "callback bodies"; Entry path routes real user callbacks to the Rust executor (not the string-descriptor register), component = stateful object binding callbacks by identity (no naming, RFC-0019 thin wrapper), typed codegen entry; retires the synthesizing `EntryNodeRuntime`; NuttX executor-callback spike validated |
| 0045 | [unified-boot-config-resolution](0045-unified-boot-config-resolution.md) | Draft | one boot-config resolver (`node_name`/`locator`/`domain`) feeding the existing `RmwConfig`→`CffiRmw::open` sink; precedence A (hosted env > baked overlay > compiled default); `BootConfig` + `ExecutorConfig::resolve` in `nros-node`, three thin call-sites (Rust boards / C / C++) fixing #98 across all boards + the C PID-name / C++ `nros_cpp` defects; single `.nros_boot_config` bake site seeding a future config-patch tool + build-time plan image. Backs #101 |
| 0046 | [launch-authoritative-node-identity](0046-launch-authoritative-node-identity.md) | Draft | node name + namespace come from the launch `<node name= namespace=>` (SSoT, rclcpp-style override), `exec=` selects the entity (Cargo bin / CMake target), code provides only a fallback default; ONE precedence rule resolved at the one shared site both languages funnel through (`Executor::node_builder`), launch identity injected per-component (W4a param rail). Stops Rust hardcoding `create_node` names; unifies with C/C++ codegen. Naming half of #105 (graph half = per-node liveliness token) |
| 0047 | [unified-sched-context-binding](0047-unified-sched-context-binding.md) | Draft | one sched-context binding mechanism across Rust/C/C++ at ROS's granularity — the **callback group**: groups are code-declared (first-class, rclcpp/rclrs shape), group→tier owned by `system.toml` by-name (NOT the package manifest → portable, RFC-0026), executor holds a config-seeded `group → sched_context` table bound at registration. phase-272 shipped the per-node degenerate case (node-name table at `node_builder`, #124/#119); phase-273 generalizes to per-callback-group (sub-node tiering) + moves group→tier off the manifest. Concurrency type (MutuallyExclusive/Reentrant) deferred. Binding only — `run_tiers`/execution (RFC-0016) untouched |
| 0048 | [cmake-ament-consumption](0048-cmake-ament-consumption.md) | Draft | a nano-ros C/C++ package is written in the **ament_cmake convention** (`find_package(nano_ros)` + `find_package(<msgs>)` + an `add_*` verb + `ament_target_dependencies` + `install` + `ament_package`) and its `CMakeLists.txt` is **byte-identical across every platform** — the board/RMW delta lives in `package.xml <export><nano_ros …/>`. Resolution is **source-backed** (nano-ros is a source distribution, #171 D2): `find_package(nano_ros)` finds the pulled checkout via `nano_ros_ROOT` + an in-tree config, no install/crates.io. `find_package(<msg_pkg>)` triggers codegen per line via `CMAKE_FIND_PACKAGE_REDIRECTS_DIR`; two verbs (`nano_ros_add_executable` for standalone entries — exe, or `add_library`-into-`app` on Zephyr — vs `nano_ros_add_node` for workspace components); toolchain via `nros setup` CMakePresets + `nros init`. Implements #171 D5; tracked by phase-287 |
| 0049 | [hierarchical-platform-board-config](0049-hierarchical-platform-board-config.md) | Stable | one knob schema, `nros-platform.toml` + `nros-board.toml` per package, fixed 4-rung ladder, native lane front-ends (Kconfig only where the host framework requires it); retires `zenoh_platforms.toml`; resolves the phase-282 promotion as zephyr platform defaults |
| 0050 | [system-model](0050-system-model.md) | Draft | **SystemModel** — one resolved, checked, YAML artifact per concrete variant (early binding: args bound, conditions gone, names FQN), produced by `play_launch resolve` from launch tree + ros-launch-manifest contracts (phases 34–35) + integrator system config; three layers (structure / contracts / execution+deployment); types in a shared `model` crate in ros-launch-manifest (both projects vendor it); resolve refuses on checker Errors, embeds warnings; nano-ros build bakes each `mcu:*` node's slice (tiers→RFC-0015/0047, budgets/QoS→endpoints, domain→RFC-0045 baked rung) and the executor gains on-target contract monitors; play_launch runtime consumes the `linux` slice — same numbers both sides of a cross-machine E2E budget. Producer-side sibling: play_launch `docs/design/system-model.md` |
| 0051 | [test-matrix-architecture](0051-test-matrix-architecture.md) | Draft | ONE declared test matrix (platform × lang × RMW × workload × {example,workspace,interop}) generating fixture rows + test lanes + deterministic port/domain assignments (injective allocator); one standard-node output checker (stock-ROS-demo behavior contract); launch via framework runner metadata over prebuilt artifacts; micro-test budget. Work: phase-295 |
| 0052 | [system-model-rtos-mapper](0052-system-model-rtos-mapper.md) | Draft | the nano-ros half of RFC-0050: one mapping table from the model's execution layer to RTOS primitives per platform (priority/sched_class, stack_bytes — fixes the FreeRTOS emit_cpp drop, core pinning, ThreadX preempt_threshold; platform-inapplicable knob in the selected target = bake-time ERROR), `class` → executor modes (real_time deadline monitor, time_triggered = existing TT window, sporadic budget fed from tier tables; `interrupt` rejected v1), contract layer → generated const monitor tables (codegen stamp-offset `max_age_ms` at take, rate/jitter per spin tick, node-path latency; play_launch rule-id vocabulary through nros-diagnostic-updater; zero flash cost when uncontracted). Ingestion = `nros codegen-system --model system_model.yaml`; `nano_ros_add_executable(... MODEL …)`; orchestration-ir TierDef converts `From` the shared sched crate (round-trip drift guard). Closes RFC-0050's deploy question: `[deploy]` lives in system.toml, play_launch resolve consumes it. Tracked by phase-296 |
| 0053 | [threadx-multi-tier-execution](0053-threadx-multi-tier-execution.md) | Draft | ThreadX gains multi-tier execution (one Executor per tier over one shared session) like freertos/zephyr/nuttx, via **codegen-baked static per-tier stacks** (Option A, over a `TX_BYTE_POOL` — chosen for exact RAM / link-time placement / no runtime-alloc failure / consistency with the other boards; the cross-RTOS norm) + a C `nros_threadx_create_task` shim, with `preempt_threshold` applied through ThreadX's **native** `tx_thread_preemption_change` — the one platform where the six-dim `non_preempt_scope` is a kernel primitive, not emulated. Each per-tier executor calls the portable `apply_tier_sched_policy` (phase-296 W5.4). Migration ladder v0 (single-executor tier policy) → A. Work: phase-297 |
| 0054 | [c-header-abi-ssot](0054-c-header-abi-ssot.md) | Stable | The hand-written C headers become the SINGLE source of truth for the RMW backend contract + platform port ABI: a new pure-header `nros-rmw-abi` package (+ `nros-platform-api` staying the platform home), with Rust consuming **committed bindgen output** (regen-diff gated; libclang maintainer/CI-only, embedded builds see plain checked-in Rust) and the hand mirrors + #238/#239 drift gates retiring. Closes the #131/#160/#238/#239 stale-mirror class by construction, incl. per-slot fn signatures. Work: phase-299 |
| 0087 | [package-identity-and-provider-format](0087-package-identity-and-provider-format.md) | Draft | A nano-ros workspace is a colcon workspace: **a package is a directory with `package.xml`, and nothing else identifies one** — not `Cargo.toml`, since an entry may be cargo- or CMake-built. nano-ros-owned packages declare `nros_cmake` / `nros_cargo` instead of borrowing ament's spelling, so a stock `colcon build` REFUSES firmware instead of trying to install it; interface packages keep `ament_cmake` because they really are ROS 2 packages. Measured drift this closes: SEVEN `<build_type>` spellings (157 `ament_cargo`, 125 `ament_cmake`, 75 `cmake`, plus 4 improvised nros ones), and a colcon extension registering 30 tasks under `ros.nros.<lang>.<platform>` — a build type NO `package.xml` declares, so that path cannot fire. Three export tags, one shape: `<nano_ros_provides>` ("I am", 52 tags in 21 files), the new general `<nano_ros_uses kind= name=/>` ("build me against"), and the bare `<nano_ros deploy= board= rmw=/>` (91 files) as sugar for it — NOT unified, because `deploy` is not a provider kind and both directions have already been confused by two separate readers. Descriptors carry only what cannot be DERIVED (6 of 11 rmw fields are convention written longhand). A vendor package is not a kind — it is an ordinary package whose build fetches, exporting through native channels (CMake targets; cargo `links` + `DEP_<LINKS>_<KEY>`, which this tree already uses), which also kills the ambient-env hazard. Adds the search path (`package_paths` + `NROS_PACKAGE_PATH`, shadowing REPORTED) and `--packages-select`/`--packages-up-to`. Refuses install prefixes, per-package isolated in-tree builds and a Python plugin ABI. Work: phase-420 |

### Domain & safety

| RFC | Doc | Status | One-liner |
| --- | --- | --- | --- |
| 0028 | [e2e-safety-protocol-integration](0028-e2e-safety-protocol-integration.md) | Stable | safety-critical platform integration analysis |
| 0029 | [zonal-vehicle-architecture](0029-zonal-vehicle-architecture.md) | Stable | zonal E/E architecture + nano-ros fit |

## Superseded

Retired RFCs live in [archived/](archived/) with `status: Superseded` and a forward pointer.

## How a decision moves: RFC → roadmap → code

1. **Draft RFC** captures the option space + open questions.
2. Discussion (PR/issue/phase doc) resolves the open questions.
3. A **phase doc** in `docs/roadmap/` carries the work items + acceptance tests and names the
   RFC in its `Implements:` header.
4. Implementation lands; the RFC flips to **Stable** and ARCHITECTURE.md is updated in the
   same commit.
