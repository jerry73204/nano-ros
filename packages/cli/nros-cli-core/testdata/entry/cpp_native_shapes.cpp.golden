// Generated by `nros codegen entry --lang cpp` (typed — RFC-0043)
//   bringup = demo_bringup
//   launch  = /tmp/system.launch.xml
//   board   = native
//
// DO NOT EDIT — regenerated at configure time. Routes each launch node
// to the real executor via its component object (no synthesizing
// interpreter); `Board::run_components` owns init/spin/shutdown.

#include <nros/boot_config.h>
#include <nros/component.hpp>
#include <nros/main.hpp>
#include <nros/nros.hpp>
#include <new> // placement-new into the component arena slot
#include <nros/component_node.hpp>

#include "talker_pkg/talker.hpp"
#include "rclcpp_pkg/rcl_one.hpp"

// C component factory + configure seam (NROS_C_COMPONENT); the
// node's `ffi_handle()` is handed to the C `configure` as an opaque
// `nros_cpp_node_t*` — the C side registers real callbacks on it.
extern "C" {
    void* __nros_c_component_c_pkg_create(void);
    int32_t __nros_c_component_c_pkg_configure(const ::nros_cpp_node_t* node, void* executor, void* self);
}


// Rust component install seam (nros::node!); the Rust node
// self-creates its node on the shared executor handle (phase-257).
extern "C" {
    int32_t __nros_component_rust_pkg_install(const void* node, void* executor, void* self);
}

// Static per-node storage (outlives the spin loop; no heap).
static ::nros::Node __nros_node_0;
static ::talker_pkg::TALKER __nros_comp_0;
static ::nros::Node __nros_node_1;
static ::talker_pkg::TALKER2 __nros_comp_1;
static ::nros::Node __nros_node_2;
static ::nros::Node __nros_node_3;
alignas(::rclcpp_pkg::RCL_ONE) static unsigned char __nros_comp_buf_6[sizeof(::rclcpp_pkg::RCL_ONE)];
static ::rclcpp_pkg::RCL_ONE* __nros_comp_6 = nullptr;

static int32_t __nros_entry_setup() {
    {
        ::nros::Result r = ::nros::create_node(__nros_node_0, "talker");
        if (!r.ok()) return static_cast<int32_t>(r.raw());
        r = __nros_comp_0.configure(__nros_node_0);
        if (!r.ok()) return static_cast<int32_t>(r.raw());
    }
    {
        ::nros::Result r = ::nros::create_node(__nros_node_1, "talker2");
        if (!r.ok()) return static_cast<int32_t>(r.raw());
        r = __nros_comp_1.configure(__nros_node_1);
        if (!r.ok()) return static_cast<int32_t>(r.raw());
    }
    {
        ::nros::Result r = ::nros::create_node(__nros_node_2, "c_one");
        if (!r.ok()) return static_cast<int32_t>(r.raw());
        void* self = __nros_c_component_c_pkg_create();
        int32_t crc = __nros_c_component_c_pkg_configure(__nros_node_2.ffi_handle(), __nros_node_2.executor_handle(), self);
        if (crc != 0) return crc;
    }
    {
        ::nros::Result r = ::nros::create_node(__nros_node_3, "c_two");
        if (!r.ok()) return static_cast<int32_t>(r.raw());
        void* self = __nros_c_component_c_pkg_create();
        int32_t crc = __nros_c_component_c_pkg_configure(__nros_node_3.ffi_handle(), __nros_node_3.executor_handle(), self);
        if (crc != 0) return crc;
    }
    {
        void* __exec = ::nros::global_handle();
        if (__exec == nullptr) return static_cast<int32_t>(::nros::ErrorCode::NotInitialized);
        int32_t crc = __nros_component_rust_pkg_install(nullptr, __exec, nullptr);
        if (crc != 0) return crc;
    }
    {
        void* __exec = ::nros::global_handle();
        if (__exec == nullptr) return static_cast<int32_t>(::nros::ErrorCode::NotInitialized);
        int32_t crc = __nros_component_rust_pkg_install(nullptr, __exec, nullptr);
        if (crc != 0) return crc;
    }
    {
        ::nros::NodeHandle __h(::nros::global_handle());
        if (!__h.valid()) return static_cast<int32_t>(::nros::ErrorCode::NotInitialized);
        __nros_comp_6 = new (__nros_comp_buf_6) ::rclcpp_pkg::RCL_ONE(__h);
        if (!__nros_comp_6->ok()) {
            ::nros::detail::report_component_failure("rcl_one", __nros_comp_6->error_what(), __nros_comp_6->error_code());
            return __nros_comp_6->error_code();
        }
    }
    return 0;
}

/* Phase 266 (RFC-0045) — baked boot config: post-link readable + session name. */
static const struct nros_baked_boot_config NROS_BOOT_CONFIG
#if defined(__GNUC__) || defined(__clang__)
__attribute__((section(".nros_boot_config"), used))
#endif
= {
.magic      = NROS_BOOT_CONFIG_MAGIC,
.version    = NROS_BOOT_CONFIG_VERSION,
.set_flags  = 0,
.domain_id  = 0,
.node_name  = "",
.locator    = "",
.namespace_ = "",
.rmw        = "",
};

int main(int /*argc*/, char** /*argv*/) {
    return ::nros::board::LinuxBoard::run_components(nros_boot_config_node_name(&NROS_BOOT_CONFIG), &__nros_entry_setup);
}
