an image whose interface dependencies are declared in more than one package now converges its derived size knobs inside ONE `west build` -- the re-configure arm used to be per producer CALL, so the second `nros_find_interfaces()` settled the date the first had armed and the build compiled at the previous pass's sizes
